<UserControl x:Class="FISApiClient.Views.ConnectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:FISApiClient.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- MDS Connection -->
            <Border Grid.Column="0"
                    Background="White" 
                    CornerRadius="6" 
                    Padding="15">
                <Border.Effect>
                    <DropShadowEffect Color="#000000" Opacity="0.1" BlurRadius="8" ShadowDepth="2"/>
                </Border.Effect>

                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <!-- MDS Header -->
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <TextBlock Text="📊 MDS" 
                                      FontSize="16" 
                                      FontWeight="Bold"
                                      Foreground="#1976D2"
                                      VerticalAlignment="Center"/>
                            <TextBlock Text=" (Market Data)" 
                                      FontSize="12" 
                                      Foreground="#666666"
                                      VerticalAlignment="Center"
                                      Margin="3,0,0,0"/>
                        </StackPanel>

                        <!-- MDS Status -->
                        <Border Background="{Binding MdsConnectionStatusColor}" 
                                CornerRadius="4" 
                                Padding="10,6"
                                Margin="0,0,0,12">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Ellipse Width="8" Height="8" Fill="White" Margin="0,0,6,0"/>
                                <TextBlock Text="{Binding MdsConnectionStatusText}" 
                                          FontSize="11" 
                                          FontWeight="SemiBold"
                                          Foreground="White"/>
                            </StackPanel>
                        </Border>

                        <!-- MDS Form -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="70"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- IP -->
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="IP:" VerticalAlignment="Center" FontSize="11" Foreground="#555" Margin="0,0,0,8"/>
                            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding MdsIpAddress}" IsEnabled="{Binding IsMdsNotConnected}" Margin="0,0,0,8"/>

                            <!-- Port -->
                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Port:" VerticalAlignment="Center" FontSize="11" Foreground="#555" Margin="0,0,0,8"/>
                            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding MdsPort}" IsEnabled="{Binding IsMdsNotConnected}" Margin="0,0,0,8"/>

                            <!-- User -->
                            <TextBlock Grid.Row="2" Grid.Column="0" Text="User:" VerticalAlignment="Center" FontSize="11" Foreground="#555" Margin="0,0,0,8"/>
                            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding MdsUser}" IsEnabled="{Binding IsMdsNotConnected}" Margin="0,0,0,8"/>

                            <!-- Password -->
                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Pass:" VerticalAlignment="Center" FontSize="11" Foreground="#555" Margin="0,0,0,8"/>
                            <PasswordBox Grid.Row="3" Grid.Column="1" x:Name="MdsPasswordBox" IsEnabled="{Binding IsMdsNotConnected}" Margin="0,0,0,8"/>

                            <!-- Node -->
                            <TextBlock Grid.Row="4" Grid.Column="0" Text="Node:" VerticalAlignment="Center" FontSize="11" Foreground="#555" Margin="0,0,0,8"/>
                            <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding MdsNode}" IsEnabled="{Binding IsMdsNotConnected}" Margin="0,0,0,8"/>

                            <!-- Subnode -->
                            <TextBlock Grid.Row="5" Grid.Column="0" Text="Subnode:" VerticalAlignment="Center" FontSize="11" Foreground="#555"/>
                            <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding MdsSubnode}" IsEnabled="{Binding IsMdsNotConnected}"/>
                        </Grid>

                        <!-- MDS Buttons -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
                            <Button Content="Połącz" 
                                   Command="{Binding ConnectMdsCommand}"
                                   Width="90"
                                   Margin="0,0,8,0"/>
                            <Button Content="Rozłącz" 
                                   Command="{Binding DisconnectMdsCommand}"
                                   Width="90"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>