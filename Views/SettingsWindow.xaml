<Window x:Class="FISApiClient.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Ustawienia" Height="200" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource WindowBackgroundBrush}">
    <Grid Margin="15">
        <StackPanel>
            <TextBlock Text="Wygląd" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,10"/>
            <CheckBox IsChecked="{Binding IsDarkMode}" 
                      Content="Użyj ciemnego motywu (Dark Mode)"
                      Foreground="{DynamicResource SecondaryTextBrush}"
                      FontSize="14"/>
        </StackPanel>
    </Grid>
</Window>
